# Pinned so CI is reproducible. Bump deliberately when adopting a newer GO-CAM
# schema (gocam) — that is what "conform to the current schema" tracks.
gocam==0.11.0
linkml==1.10.0
oaklib==0.7.4
PyYAML>=6.0
pronto>=2.5.0  # force a Python-3.10+-compatible pronto (oaklib otherwise resolves ancient 0.12.2)
