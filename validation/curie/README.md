CURIE → IRI maps used by the TTL cross-check. Vendored verbatim from minerva
(`minerva-core/src/main/resources/{obo_context,go_context}.jsonld`), which is
what minerva itself uses to expand identifiers, so the drop-box expands them the
same way. `go_context` is loaded after `obo_context` (minerva's order).
